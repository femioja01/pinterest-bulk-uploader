services:
  pinterest-uploader:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - ./data:/app/data
    env_file: .env
    restart: unless-stopped
    environment:
      - TZ=Africa/Lagos
