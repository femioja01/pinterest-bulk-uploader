services:
  pinterest-uploader:
    build: .
    ports:
      - "${PORT:-8080}:8080"
    volumes:
      - ./data:/app/data
    restart: unless-stopped
    environment:
      - PORT=8080
      - TZ=Africa/Lagos
